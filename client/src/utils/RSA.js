// const genKey = (bits) => {
//     return '123';
// };

const encrypt = (key, data) => {
    return '123';
};

// const decrypt = (key, encryptedData) => {
//     return '123';
// };

export default {
    // genKey,
    encrypt,
    // decrypt,
};
